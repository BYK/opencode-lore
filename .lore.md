<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e1349-63c2-72f1-8725-db0ee29efedd -->
* **3-tier session identification with header learning**: Session identification uses 3-tier hierarchy with tier-aware logging: (1) Known headers - \`x-claude-code-session-id\`, \`x-session-affinity\`, \`x-parent-session-id\`; (2) Learned headers - collect \`x-\` headers with ID-like values during fingerprint bootstrap, promote after stability validation; (3) Fingerprint fallback. \`identifySession()\` returns \`{sessionId, tier}\` object. Implementation uses \`headerToSessionMap\` for O(1) reverse lookup. Logging includes tier for debugging: "Session abc123 identified via tier-1 (known headers)". Higher tiers override lower tiers.

<!-- lore:019e1337-90c3-7e57-a1f4-64e0445f0475 -->
* **Claude Code compaction mechanism and triggers**: Claude Code compaction triggers at ~8192 tokens via tiktoken cl100k\_base counting. Process: (1) Special system prompt requests conversation summary; (2) LLM response becomes new first user message; (3) All original history discarded via \`createCompactBoundary()\` with timestamp. Complete history replacement in single operation. Files: \`compact.ts\`, \`query.ts\`, \`inProcessRunner.ts\`. Session ID persists across compaction - no session fragmentation.

<!-- lore:019e133c-1e2a-72b4-97c9-75965fe2d0f6 -->
* **Claude Code sends persistent session ID in HTTP headers**: Claude Code sends persistent \`X-Claude-Code-Session-Id\` header with UUID generated at startup. Session ID persists across compaction, model changes, and entire CLI session lifecycle. Header normalizes to lowercase: \`rawHeaders\["x-claude-code-session-id"]\`. Sub-agents share parent session ID, enabling direct session identification without fragile fingerprinting. Source: Claude Code client.ts line 116.

<!-- lore:019e1330-a34e-7917-9802-35093f041a31 -->
* **Data management pattern across CLI and web UI**: Data management follows consistent pattern: core/data.ts exports unified APIs (listProjects, clearProject, etc), CLI data.ts provides terminal interface, gateway ui.ts serves web forms. Clear operations regenerate .lore.md and warn to commit changes. Both CLI and web UI use same core APIs for consistency.

<!-- lore:019e1328-d565-7f53-bc81-915c48531370 -->
* **Gateway HTTP server uses Bun.serve with manual routing**: Gateway implements HTTP server using Bun.serve() native API on port 6969 (LORE\_LISTEN\_PORT). Manual routing via if/else chain in fetch() handler. Routes: POST /v1/messages (Anthropic), POST /v1/chat/completions (OpenAI), GET /v1/models, GET /health. Uses helper functions jsonResponse(), errorResponse() for JSON responses. No web framework dependency.

<!-- lore:019e1352-b81c-7997-8006-ab66063c4b32 -->
* **Multiple host binding via Bun.serve instance spawning**: Gateway supports binding to multiple IP addresses by spawning one \`Bun.serve()\` instance per host, since Bun.serve() only accepts single hostname. Config parses comma-separated \`LORE\_HOST\` env var and multiple \`--host\` CLI flags into \`hosts: string\[]\`. Server spawns array of servers, returns combined stop functions. Agent connections always use first host (typically 127.0.0.1) for local access. All instances share same port and handler logic.

<!-- lore:019e133d-7e72-7e17-a831-11d300437ab9 -->
* **OpenCode sends multiple session identifiers in HTTP headers and metadata**: OpenCode sends session identifiers in multiple HTTP headers: (1) \`x-session-affinity\` with persistent SessionID for load balancing - this is the primary session identifier; (2) \`x-parent-session-id\` for sub-agent requests linking to parent session; (3) \`x-opencode-session\` with UUID; (4) \`session\_id\` for GitHub Copilot; (5) \`x-opencode-client\` with version info; (6) OpenAI \`experimental\_telemetry\` metadata; (7) Azure \`promptCacheKey\`. Combined with Claude Code's \`x-claude-code-session-id\`, these headers enable direct session identification without fingerprinting, solving compaction/model-change issues.

<!-- lore:019e132b-8785-76e0-85b2-0b5903aa4087 -->
* **SQLite schema with FTS5 search across messages, distillations, knowledge**: SQLite schema at $XDG\_DATA\_HOME/opencode-lore/lore.db with 5 tables: \`projects\` (path->UUID), \`temporal\_messages\` (conversation history), \`distillations\` (compressed summaries), \`knowledge\` (LTM entries), \`lat\_sections\` (.lore.md cache). All content tables have FTS5 search and project\_id foreign key. Projects auto-created on first access. WAL journal mode, foreign keys ON. Runtime drivers: bun:sqlite native, node:sqlite with .query() compatibility.

<!-- lore:019e132b-1b18-7723-9586-be04f1b16627 -->
* **Temporal-distillation-knowledge data flow lifecycle**: Data flows through 4 stages: (1) Temporal messages - raw conversation with FTS5, TTL+size pruning; (2) Distillations gen-0 - LLM-compressed 5-30 message segments; (3) Distillations gen-1+ - meta-distillations consolidating gen-0, archive gen-0; (4) Knowledge (LTM) - curated entries from curator + pattern-extract. Knowledge exported to .lore.md for version control. Context transform injects distillation prefix + LTM entries into system prompt.

<!-- lore:019e1323-ffdb-7556-9624-5fa69af1afec -->
* **urgentDistillation singleton causes cross-session race conditions**: urgentDistillation is module-level singleton with consume-on-read pattern. Three async consumers (gateway, opencode, distillation service) race for the flag across all sessions. First caller gets true, others get false even if their session needs distillation. Requires per-session tracking to fix race condition.

### Gotcha

<!-- lore:019e1352-4391-79d2-9cc8-1aa6e05afea8 -->
* **Bun.serve hostname limitation requires multiple instances for multi-IP binding**: Bun.serve() only accepts a single hostname string, not an array. To bind to multiple IP addresses, must spawn separate server instances - one per IP. Gateway should spawn multiple Bun.serve() calls and return array of stop functions. Config layer parses comma-separated LORE\_LISTEN\_HOST env var into hosts array. CLI accepts multiple --host flags. For agent connections, always use 127.0.0.1 or first host since agents connect locally.

<!-- lore:019e1322-eb6a-7660-8975-c1d2e91096cf -->
* **fastembed ARM64 compatibility via tokenizers override**: fastembed 2.1.0 pins \`@anush008/tokenizers\` to \`^0.0.0\`, breaking ARM64 which needs 0.5.0+. Fix: override with \`"@anush008/tokenizers": "^0.6.0"\` in package.json. Clear vendor cache after overrides: \`rm -rf /tmp/fastembed\_cache\`. 0.6.0+ includes ARM64 binaries.

<!-- lore:019e135e-e3fa-7b07-8eb0-c8d983860125 -->
* **Fingerprint model parameter causes unnecessary session fragmentation**: Original fingerprint included \`model\` parameter, causing new sessions on model changes within same conversation. Fixed by removing model from fingerprint generation - only use \`firstUserContent + authSuffix\`. Model changes should not break session continuity since conversation context remains semantically the same. Session identification should be model-agnostic.

<!-- lore:019e135e-6e7e-7ea9-9410-df480e406058 -->
* **Fingerprint must exclude model parameter to prevent session breaks**: Session fingerprint should exclude \`model\` parameter to prevent session fragmentation on model changes. Users frequently switch models mid-conversation expecting continuity. Fingerprint composition: \`firstUserContent + authSuffix\` only. Remove model from both fingerprint generation and all callers in pipeline.ts. Model changes should not break session continuity - session identity is about user context, not AI model choice.

<!-- lore:019e1323-ffde-75c3-9af8-d52ac068fe87 -->
* **OpenCode distillation threshold skips layer 1 entirely**: OpenCode plugin only triggers backgroundDistill() for result.layer >= 2, skipping layer 1 entirely. Combined with layer 1 not setting urgentDistillation flag, layer 1 contexts never get background knowledge extraction. Fix: change threshold to layer >= 1 and ensure layer 1 sets urgentDistillation flag.

### Pattern

<!-- lore:019e135d-f964-7f2f-8439-e11a62f30dd2 -->
* **Fingerprint excludes model parameter for session stability**: Session fingerprinting excludes \`model\` parameter to prevent session fragmentation during model switches. Fingerprint uses only \`firstUserContent + authSuffix\` for stable session identification across model changes and compaction events. Model parameter removed from \`createFingerprint()\` calls in pipeline - only auth context and initial user content determine session identity. Enables persistent sessions regardless of LLM model selection.

<!-- lore:019e132b-1b1f-798a-ae69-71a7f40bdeb0 -->
* **Lore.md file managed via UUID-tracked markdown with import/export cycle**: \`.lore.md\` contains project knowledge as markdown bullets with \`\<!-- lore:UUID -->\` tracking markers. \`exportLoreFile()\` writes DB entries grouped by category. \`importLoreFile()\` parses bullets: known UUID updates content, unknown UUID creates with that ID, no UUID creates new UUIDv7. \`shouldImportLoreFile()\` detects changes via content hash. Enables cross-machine knowledge sync via git commits.

<!-- lore:019e1359-6546-740a-b765-b43239a32be1 -->
* **Lore.md merge conflict resolution via keeping all unique entries**: When \`.lore.md\` has merge conflicts, take both sides since entries are auto-generated from database and all valid knowledge is worth preserving. The file structure with UUID markers makes duplicate detection automatic during import. Pattern: accept all conflicting entries, let import process handle deduplication. Commit merged result - database will normalize on next export cycle.

<!-- lore:019e1352-b820-7e0c-b064-8d6caadc19fc -->
* **Multiple bind addresses via Bun.serve() array pattern**: Bun.serve() only accepts single hostname, but can spawn multiple server instances for multiple bind addresses. Config parses comma-separated LORE\_HOST env var into hosts array. Server spawns one Bun.serve() per host, returns array of stop functions. CLI accepts multiple --host flags. Agent connections always use first host (127.0.0.1) since agents connect locally, while external clients can use any configured address.

<!-- lore:019e1351-cdc0-7416-af76-3f7fe16fb47d -->
* **Multiple host binding requires spawning separate server instances**: Bun.serve() only accepts single hostname parameter. To bind to multiple IP addresses, spawn one server instance per host using Promise.all(). Return array of stop functions for cleanup. Pattern: parse comma-separated LORE\_LISTEN\_HOST env var into hosts array, accept multiple --host CLI flags, then map each host to separate Bun.serve() call with same port and handler.
