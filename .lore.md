<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e1348-793e-7e47-8b68-d18bf3455a0c -->
* **3-tier session identification hierarchy with header learning**: Session identification follows 3-tier hierarchy: (1) Known headers - \`x-claude-code-session-id\` (Claude Code), \`x-session-affinity\` (OpenCode), \`x-parent-session-id\` (sub-sessions merge to parent); (2) Learned headers - after 3 turns with stable ID-like values in pattern-matched \`x-\` headers (\`/session/\` or \`/affinity/\` names), promote as session ID; (3) Fingerprint fallback - \`firstUserContent + authSuffix\` (no model) for bootstrap. Higher tiers take priority. Eliminates compaction/model-change session breaks.

<!-- lore:019e1349-63d2-7866-882d-84eaf200d4d9 -->
* **3-tier session identification strategy with header learning**: Gateway session identification uses 3-tier strategy: (1) Known headers (\`x-claude-code-session-id\`, \`x-session-affinity\`) for immediate match; (2) Learned headers discovered during fingerprint bootstrap - collect candidate \`x-\` headers with ID-like values, promote after 3 stable turns within session but different across sessions; (3) Fingerprint fallback (firstUserContent + authSuffix, no model). Cross-session value tracking prevents promoting global constants like client versions. Header-based identification eliminates compaction/model-change session fragmentation.

<!-- lore:019e1348-7948-703c-822a-78463826f0cc -->
* **3-tier session identification system replacing fingerprinting**: Gateway uses 3-tier session ID extraction: (1) Known headers - \`x-claude-code-session-id\`, \`x-session-affinity\` for immediate match; \`x-parent-session-id\` links sub-agents to parent; (2) Learned headers - pattern match \`/^x-.\*session/i\` and \`/^x-.\*affinity/i\`, promote candidates stable within session but varying across sessions after 3 turns; (3) Fingerprint fallback - \`firstUserContent + authSuffix\` (no model) for bootstrap/unknown clients. Higher tiers override lower. Filters out global constants that appear identical across all sessions.

<!-- lore:019e1349-63c2-72f1-8725-db0ee29efedd -->
* **3-tier session identification with header learning**: Session identification uses 3 tiers: (1) Known headers - immediate match on \`x-claude-code-session-id\` (Claude Code), \`x-session-affinity\` (OpenCode), \`x-parent-session-id\` (merge with parent); (2) Learned headers - during first 3 fingerprinted turns, collect candidate \`x-\` headers with ID-like values (8-128 chars, alphanumeric+dash/underscore), pattern-match \`/^x-.\*session/i\` and \`/^x-.\*affinity/i\`, promote headers that are stable within session AND vary across sessions (filters out global constants like client version); (3) Fingerprint fallback - \`firstUserContent + authSuffix\` without model for bootstrap. Priority: Tier 1 > Tier 2 > Tier 3.

<!-- lore:019e1337-90c3-7e57-a1f4-64e0445f0475 -->
* **Claude Code compaction mechanism and triggers**: Claude Code auto-compaction triggers at ~8192 token threshold. Process: (1) Special compaction request with system prompt asking Claude to summarize conversation; (2) LLM response becomes new first user message; (3) All original history discarded. Complete history replacement in single operation via \`createCompactBoundary()\` with timestamp. Token counting via tiktoken cl100k\_base. Files: \`compact.ts\`, \`query.ts\`, \`inProcessRunner.ts\`.

<!-- lore:019e133c-1e2a-72b4-97c9-75965fe2d0f6 -->
* **Claude Code sends persistent session ID in HTTP headers**: Claude Code sends persistent \`X-Claude-Code-Session-Id\` header with UUID generated at startup. Session ID persists across compaction, model changes, and entire CLI session lifecycle. Header normalizes to lowercase: \`rawHeaders\["x-claude-code-session-id"]\`. Sub-agents share parent session ID, enabling direct session identification without fragile fingerprinting. Source: Claude Code client.ts line 116.

<!-- lore:019e1330-a34e-7917-9802-35093f041a31 -->
* **Data management pattern across CLI and web UI**: Data management follows consistent pattern: core/data.ts exports unified APIs (listProjects, clearProject, etc), CLI data.ts provides terminal interface, gateway ui.ts serves web forms. Clear operations regenerate .lore.md and warn to commit changes. Both CLI and web UI use same core APIs for consistency.

<!-- lore:019e1328-d565-7f53-bc81-915c48531370 -->
* **Gateway HTTP server uses Bun.serve with manual routing**: Gateway implements HTTP server using Bun.serve() native API on port 6969 (LORE\_LISTEN\_PORT). Manual routing via if/else chain in fetch() handler. Routes: POST /v1/messages (Anthropic), POST /v1/chat/completions (OpenAI), GET /v1/models, GET /health. Uses helper functions jsonResponse(), errorResponse() for JSON responses. No web framework dependency.

<!-- lore:019e133d-7e72-7e17-a831-11d300437ab9 -->
* **OpenCode sends multiple session identifiers in HTTP headers and metadata**: OpenCode sends session identifiers in multiple HTTP headers: (1) \`x-session-affinity\` with persistent SessionID for load balancing - this is the primary session identifier; (2) \`x-parent-session-id\` for sub-agent requests linking to parent session; (3) \`x-opencode-session\` with UUID; (4) \`session\_id\` for GitHub Copilot; (5) \`x-opencode-client\` with version info; (6) OpenAI \`experimental\_telemetry\` metadata; (7) Azure \`promptCacheKey\`. Combined with Claude Code's \`x-claude-code-session-id\`, these headers enable direct session identification without fingerprinting, solving compaction/model-change issues.

<!-- lore:019e132b-8785-76e0-85b2-0b5903aa4087 -->
* **SQLite schema with FTS5 search across messages, distillations, knowledge**: SQLite schema at $XDG\_DATA\_HOME/opencode-lore/lore.db with 5 tables: \`projects\` (path->UUID), \`temporal\_messages\` (conversation history), \`distillations\` (compressed summaries), \`knowledge\` (LTM entries), \`lat\_sections\` (.lore.md cache). All content tables have FTS5 search and project\_id foreign key. Projects auto-created on first access. WAL journal mode, foreign keys ON. Runtime drivers: bun:sqlite native, node:sqlite with .query() compatibility.

<!-- lore:019e132b-1b18-7723-9586-be04f1b16627 -->
* **Temporal-distillation-knowledge data flow lifecycle**: Data flows through 4 stages: (1) Temporal messages - raw conversation with FTS5, TTL+size pruning; (2) Distillations gen-0 - LLM-compressed 5-30 message segments; (3) Distillations gen-1+ - meta-distillations consolidating gen-0, archive gen-0; (4) Knowledge (LTM) - curated entries from curator + pattern-extract. Knowledge exported to .lore.md for version control. Context transform injects distillation prefix + LTM entries into system prompt.

<!-- lore:019e1323-ffdb-7556-9624-5fa69af1afec -->
* **urgentDistillation singleton causes cross-session race conditions**: urgentDistillation is module-level singleton with consume-on-read pattern. Three async consumers (gateway, opencode, distillation service) race for the flag across all sessions. First caller gets true, others get false even if their session needs distillation. Requires per-session tracking to fix race condition.

### Gotcha

<!-- lore:019e1322-eb6a-7660-8975-c1d2e91096cf -->
* **fastembed ARM64 compatibility via tokenizers override**: fastembed 2.1.0 pins \`@anush008/tokenizers\` to \`^0.0.0\`, breaking ARM64 which needs 0.5.0+. Fix: override with \`"@anush008/tokenizers": "^0.6.0"\` in package.json. Clear vendor cache after overrides: \`rm -rf /tmp/fastembed\_cache\`. 0.6.0+ includes ARM64 binaries.

<!-- lore:019e1348-794d-7dbe-b615-9baee9a45791 -->
* **Header learning must check cross-session stability to avoid false positives**: When learning session headers from unknown clients, headers stable within one session might still be global constants (client version, auth hash). True session IDs are stable within session but unique across sessions. Learning algorithm must track header values globally and only promote candidates that show different values in different fingerprinted sessions. Single-value headers across all sessions are likely not session identifiers.

<!-- lore:019e1323-ffde-75c3-9af8-d52ac068fe87 -->
* **OpenCode distillation threshold skips layer 1 entirely**: OpenCode plugin only triggers backgroundDistill() for result.layer >= 2, skipping layer 1 entirely. Combined with layer 1 not setting urgentDistillation flag, layer 1 contexts never get background knowledge extraction. Fix: change threshold to layer >= 1 and ensure layer 1 sets urgentDistillation flag.

### Pattern

<!-- lore:019e132b-1b1f-798a-ae69-71a7f40bdeb0 -->
* **Lore.md file managed via UUID-tracked markdown with import/export cycle**: \`.lore.md\` contains project knowledge as markdown bullets with \`\<!-- lore:UUID -->\` tracking markers. \`exportLoreFile()\` writes DB entries grouped by category. \`importLoreFile()\` parses bullets: known UUID updates content, unknown UUID creates with that ID, no UUID creates new UUIDv7. \`shouldImportLoreFile()\` detects changes via content hash. Enables cross-machine knowledge sync via git commits.
